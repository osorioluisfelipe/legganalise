<div class="container">
  <ul>
    <% @samples.each do |sample| %>
      <li>
        <p><%= sample.sample_name %></p>

          <p><%= link_to 'Select Analysis', new_sample_sample_analysis_path(sample.id)  %></p>

          <ul> Análises solicitadas:
          <% SampleAnalysis.where(sample_id: sample.id).each do |sample_analysis| %>
            <li><%= sample_analysis.lab_analysis.analysis_name %></li>
          <% end %>
          </ul>

      </li>
    <% end %>
  </ul>

  <div>
    <%= render 'shared/new_sample' %>
  </div>



  <%= link_to 'Delete request', request_path(@request), method: :delete, data: { confirm: 'Are you sure do you wanto to delete this request?' } %>
  <%= link_to 'Back to requests', requests_path %>
</div>
